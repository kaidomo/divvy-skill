---
name: divvy
description: 들어온 일을 두 러너(Claude Code · Codex CLI)에 배정하고, Codex 몫은 실제로 `codex exec`로 던져 결과를 회수하는 분배기. primary는 도구 게이트 → 브리프 비용 → 적성(ROSTER)으로 고르고, 오판 비용은 별도로 검토자를 붙인다(검토자는 항상 primary가 아닌 러너). "분배해줘", "이건 누가 풀어", "누가 잘 풀지", "코덱스한테 돌려", "코덱스로 넘겨", "divvy", "이 작업들 나눠줘", "dispatch", "누구한테 시킬까" 요청에 트리거. 티어·추론량 판정(모델 중립)은 `modelchk`, 산출물 교차리뷰 루프는 `peer-review`가 정본 — 이 스킬은 **누가 할지**만 정하고 실행을 붙인다.
---

한 사람이 두 러너를 동시에 물고 있을 때, 일마다 **누가 할지**를 정하고 그 배정대로 **실제로 돌린다**.

## 무엇이 아닌가 (경계)

- **`modelchk`이 아니다.** modelchk은 모델 중립으로 티어(fast/standard/frontier)·추론량을 판정한다. divvy는 그 판정을 **이 기계의 구체 러너**에 묶는다. 필요하면 divvy가 modelchk을 먼저 호출해 결과를 입력으로 쓴다(중복 판정 금지).
- **`peer-review`가 아니다.** "산출물을 교차리뷰"가 답이면 divvy는 **검토자만 지정하고 peer-review로 넘긴다**(라운드·finding_id·triage 계약은 그 스킬 소유). divvy가 리뷰 프로토콜을 재구현하지 않는다.
- **작업을 대신 판단해주지 않는다.** divvy는 배정과 회수만 한다. 배정된 일 자체의 품질 게이트는 그 일의 스킬·검증이 소유한다.

## 러너 2개

데이터 정본은 [`ROSTER.md`](./ROSTER.md) — 확신도가 `[사실]`(관찰된 도구) / `[도출]`(도구 사실에서 필연적으로 따라옴 = 한쪽이 못 한다) / `[실측]`(LEDGER 3건 이상으로 확인된 우위) / `[가설]`(측정 없음) 네 등급으로 나뉘어 있고, 판정은 **행 ID로 인용**한다. 승격 근거는 [`LEDGER.md`](./LEDGER.md)가 쌓는다.

| | CLAUDE (Claude Code, 이 세션) | CODEX (`codex-cli`, 헤드리스) |
|---|---|---|
| 고유 자산 | 이 세션의 맥락·메모리, `~/.claude/skills` 생태계, MCP(Jira·Confluence·Figma), 브라우저 자동화, 서브에이전트 팬아웃, gh/PR 관례 | 독립 모델(내 사각을 보는 눈), 샌드박스 실행, 비대화 배치(`--output-schema`·`-o`·`--json`), `codex review`, 장시간 무인 단일리포 루프 |
| 접근 못 하는 것 | (해당 없음 — 이 세션이 기준) | 이 세션의 맥락·메모리, 스킬, 설정된 MCP, 브라우저, 팬아웃 |
| 과금 | 20x 정액 | 20x 정액 |

**과금이 양쪽 다 정액이라 비용은 판정 기준이 아니다.** 남는 기준은 적성·독립성·유휴다.

> 공개 배포본의 ROSTER는 예시 환경에서 관찰한 기본값이다. 처음 설치했거나 러너·요금제·도구 구성이
> 달라졌다면 `ROSTER.md`의 T행을 현재 환경에서 다시 확인한다. 특히 양쪽 정액 구독이 아니면 T-10과
> 비용 비기준 가정을 그대로 사용하지 않는다.

## 판정 — primary(G1~G3) + 보증 위상(G4)

배정은 **두 질문**이다: 누가 하나(primary), 한 명으로 충분한가(보증). 앞의 셋이 primary를 고르고, **G4는 primary가 어디서 정해졌든 항상 따로 평가한다** — G1에서 갈렸다고 G4를 건너뛰면 고위험 작업이 단독 수행으로 나간다.

### primary 선택 — 처음 갈린 게이트에서 멈춘다

**G1 도구 게이트 (hard).** **한쪽이 아예 못 하는** 도구가 필요하면 거기서 끝. **근거는 `T-n`으로 인용한다.**

- CLAUDE로 갈리는 경우: 세션 맥락·메모리(T-1), 스킬(T-2), 설정된 MCP(T-3), 브라우저(T-4), 팬아웃(T-5).
- CODEX로 갈리는 경우: **비대화·무인 실행이 본질**일 때(T-8, 그리고 승인 프롬프트 없는 샌드박스 실행 = T-6의 비대화 부분).
- **양쪽 다 되는 능력은 G1에서 갈리지 않는다.** 구조화 출력(T-7)은 양쪽에 있으므로 그 자체로는 hard gate가 아니다 — 무인 배치와 결합될 때만(T-8) CODEX 쪽으로 간다.

**G2 브리프 비용 (hard).** CODEX로 보내려면 맥락을 프롬프트로 옮겨야 한다. 두 축을 **따로, 상호배타적으로** 측정해 표에서 읽는다.

- *브리프 크기*: 파일 경로·결정 목록만으로 되면 **짧음**. 세션에서 합의된 맥락을 산문으로 **20줄 넘게** 재구성해야 하면 **큼**. 그 사이는 **보통**.
- *실행 길이*: **패스 수로만** 정한다. 한 번의 패스로 끝나면(시행착오·재실행 불필요) **짧음**, 두 번 이상의 패스나 시행착오가 필요하면 **김**. 대상 파일 수는 이 축에 넣지 않는다 — 단일 패스 다파일 작업이 두 칸에 동시에 속하게 된다. 파일 수가 판단에 필요하면 별도 메모로 남긴다.

| | 실행 짧음 | 실행 김 |
|---|---|---|
| 브리프 짧음·보통 | 갈리지 않음 → G3로 | **CODEX** (브리프 한 번, 실행 이득 큼) |
| 브리프 큼 | **CLAUDE** (옮기는 비용이 이득을 먹음) | **판단 칸** — 아래 재분류 절차를 밟는다 |

*판단 칸 절차*: 브리프를 실제로 줄여 써 본다(계약·파일 경로·결정 목록으로 압축). **줄인 브리프가 self-contained이고 크기 경계(산문 20줄 이하)를 통과했을 때만** CODEX로 재분류하고, 측정값을 줄인 뒤 값으로 다시 적는다. 통과하지 못하면 CLAUDE다. "줄일 수 있을 것 같다"는 재분류 근거가 아니다.

측정값(브리프 줄 수·실행 패스 수)을 판정표에 적는다. 라벨만 적으면 다음 사람이 재현할 수 없다.

> `[가설]` 코덱스가 유휴가 되는 주된 원인은 실력이 아니라 이 비용이다(README도 같은 등급으로 표시한다). 실측 근거는 아직 없다(LEDGER 0건). 그래서 G2는 라벨과 함께 **측정값**을 남기고, LEDGER는 **결정출처별 건수와 미위임 사유**를 따로 센다 — 원인은 그 집계가 쌓인 뒤에 말한다(경쟁 원인: G1 도구 편중·작업 구성·노출 보류).

**G3 적성 (ROSTER).** G1·G2가 안 갈랐으면 ROSTER 행으로 맞춘다. **근거는 `C-n`/`K-n`으로 인용한다.**

- 일치한 **모든** 행을 적는다(하나만 골라 적으면 충돌이 숨는다).
- 우선순위는 **세 계층**이다: `P1 = [사실]·[도출]` > `P2 = [실측]` > `P3 = [가설]`.
  (불가능은 측정을 이긴다 — `[도출]`은 "한쪽이 못 한다"이고, `[실측]`은 "이쪽이 더 나았다"다.)
- 반대 러너를 지지하는 행 중 **가장 높은 계층**만 비교한다. 그 계층에 양쪽 행이 다 있으면 —
  등급이 서로 달라도(예: 한쪽 `[사실]`, 다른 쪽 `[도출]`) — **G3는 무결정**이고 타이브레이커로 내려간다.
  같은 계층 안에서는 개수로도, 등급 이름으로도 이기지 않는다.

**타이브레이커.** G1~G3가 갈리지 않으면 **CODEX**. 근거: 과금이 동률이고, 독립 관점이 순이득이고, 유휴 러너를 깨우는 것이 이 분배기의 목적이다. 단 **G2에서 CLAUDE로 갈리지 않은 경우에만** 적용한다.

### G4 보증 위상 — primary와 별개로 항상 평가한다

오판 비용이 클수록 러너를 늘린다. **검토자는 항상 primary가 아닌 러너다**(자기검토는 보증이 아니다).

| 오판 비용 | 위상 | 검토자 |
|---|---|---|
| 高 (되돌리기 어려움·외부 노출·규제·릴리스) | primary 수행 → **독립 검토** → 사람 게이트 | primary의 반대 러너. **`peer-review`로 위임**하고 검토자를 명시(CLAUDE가 primary면 Codex 리뷰, CODEX가 primary면 `handoff.py prompt --reviewer claude-code`) |
| 中 | primary 수행 + 검토 1회 | primary의 반대 러너 |
| 低 | primary 단독 | 없음 |

**선행조건 두 개.** 中·高인데 아래 중 하나라도 불충족이면 검토를 돌릴 수 없다.

1. *검토 가능한 근거* — 검토자가 판단할 근거를 self-contained로 만들 수 있어야 한다. primary가 CLAUDE로 갈린 이유가 T-1~T-5(맥락·MCP·브라우저)라면 CODEX는 그 근거에 접근하지 못할 수 있다.
2. *노출 승인* — 반대 러너에게 넘기는 것이 노출면을 늘린다(T-11). 비공개·규제 자료는 사람 승인이 있어야 한다.

**불충족이면 `G4 blocked`로 종료한다 — 검토 완료로 처리하지 않는다.** 선택지는 셋이고, 무엇을 택했는지 LEDGER에 적는다.

- ① 근거를 self-contained로 만들 수 있으면 만든 뒤 재시도(예: 화면·티켓 내용을 발췌해 브리프에 넣는다).
- ② 사람이 직접 검토한다(가장 강한 대체).
- ③ 같은 러너의 fresh-context 검토로 대체한다. **경로가 primary에 따라 다르다** — CLAUDE primary면 `cross-functional-review`(역할 패널), CODEX primary면 별도 `codex exec`/`codex review` 컨텍스트를 새로 띄운다(Codex는 T-2상 Claude 스킬에 접근할 수 없으므로 `cross-functional-review`를 쓸 수 없다). 어느 쪽이든 **독립성이 약한 대체임을 명시한다** — 같은 모델이라 사각이 겹친다. 이것을 "독립 검토 완료"로 적지 않는다.

## 워크플로

1. 들어온 것을 **작업 단위로 쪼갠다**(하나짜리면 1건). 단위 = 하나의 러너에 통째로 줄 수 있는 최소 덩이.
2. 단위마다 primary를 정한다 — **두 갈래다.**
   - *사용자가 러너를 지정했으면*: 그 지정이 primary이고 `결정출처 = user`다. **게이트를 적용하지 않는다**(사후 정당화 금지).
   - *지정이 없으면*: G1 → G2 → G3로 정한다(처음 갈린 데서 멈춤). 안 갈리면 타이브레이커.
   두 갈래 모두 **이어서 G4를 따로 평가**한다.
3. 판정표를 낸다(아래 산출물 형식). 여기까지가 조언이고, 아직 아무것도 돌리지 않았다.
4. **위임 전 노출 확인 (사람 게이트).** 러너를 늘리면 노출면도 늘어난다(T-11). 비공개로 격리된 자료(`~/private/` 등), 규제·개인정보 산출물은 자동 위임하지 않고 **사람에게 묻고, 답을 LEDGER의 `노출확인` 칸에 적는다**. 스크립트는 이 판단을 대신하지 못한다 — 자료 분류는 내용 판단이고 토큰 검사로는 위조되기 때문이다. 스크립트가 강제하는 것은 쓰기 권한 승인(`DIVVY_WRITE_APPROVED=1`) 하나다.
5. CODEX 몫을 실행한다: 단위마다 브리프를 파일로 쓰고 `scripts/dispatch.sh`로 던진다. 여러 건이면 순차(병렬 팬아웃은 peer-review의 다렌즈 스크립트가 소유). **브리프·회수물은 `~/claude_codex_review/<주제>/`에 리뷰·위임 기록으로 두되, 이후 세션이 계속 갱신·참조할 작업 정본은 거기서 작성하거나 유지하지 않는다.** 기존 정본을 넘길 때는 작업 폴더 원본에서 필요한 사본만 스테이징한다. 새 정본 후보가 회수물로 생성되면 이를 위임 기록으로 취급하고, 검토·채택한 내용을 작업 폴더의 정본에 작성·적용한다. 잘못 만든 정본은 작업 폴더로 옮기고 기존 위치에는 `MOVED.md`를 남긴다(2026-07-31 실측 재발 사례로 규약화, peer-review "Packet folder discipline"과 같은 규칙).
6. CLAUDE 몫을 내가 수행한다.
7. 회수·보고: CODEX 산출물 경로, 요약, **성공 판정 3조건**(종료코드 0 · 파일 존재 · 공백 아님) 결과. 실패는 실패로 보고한다("못 찾았다"와 "돌지 않았다"를 구분).
   **그리고 네 번째 확인은 기계가 못 한다 — 산출물이 요청에 답했는지 읽는다.** rc 0 · 파일 존재 · 공백 아님을 모두 통과하면서 내용은 "작업 불가"인 회수가 실제로 나온다(2026-07-30 실측: Codex의 훅·플러그인 preflight가 런을 중단시키고 "리뷰 불가" 한 줄을 남겼다. `--ignore-user-config`로 재실행해 해결). 이런 회수는 **성공이 아니라 재실행 또는 CLAUDE 되돌리기**다.
8. [`LEDGER.md`](./LEDGER.md)에 `L-<nn>` 한 줄씩 기록한다(적용 행·primary·검토자·결과). **우열은 내가 채점하지 않는다** — 사람이 표시한다.

## 실행 규약

```bash
scripts/dispatch.sh <브리프파일> <출력파일> [작업디렉터리]
```

- `danger-full-access`는 지원하지 않는다(rc 2) — 샌드박스가 없으면 divvy가 잠금·백업을 지킬 수 없다.
- 기본 `--sandbox read-only`. **파일 수정이 필요하면 Codex에게 diff를 내게 하고 내가 적용한다**(사람이 검토할 수 있는 상태 유지). read-only가 아닌 샌드박스로 실제 실행하려면 `DIVVY_WRITE_APPROVED=1`이 필요하고, 없으면 스크립트가 거부한다(rc 5).
- 기본 effort `medium`. 레포 실측(`peer-review/scripts/multi_lens_review.sh` 주석)에 따르면 소·중형은 medium=high Δ=0이므로 올리지 않는다. 대형·복잡 건만 `CODEX_EFFORT=high`.
- **경로 계약**: 인자 경로는 호출한 PWD 기준으로 절대화되지만, **브리프 본문의 상대경로는 작업디렉터리 기준**으로 Codex가 해석한다(`--cd`). 브리프에는 작업루트 기준 상대경로만 쓴다.
- **최소 산출 계약**: 브리프에 `<!-- DIVVY_EXPECT: 문자열 -->`을 넣으면 `dispatch.sh`가 산출물에 그 고정 문자열이 있는지 검사한다. 마커의 콜론·비어 있지 않은 문자열·닫는 `-->`는 **같은 행**에 있어야 하고, 마커가 있는 브리프는 UTF-8이어야 한다. 마커가 여러 개면 **전부** 충족해야 한다. 문법 오류·UTF-8 오류·미충족은 rc 9이며 이번 실행의 산출물(캡처 파일)을 남기지 않는다. 마커가 없으면 기존 성공 3조건만 적용한다.
- `DRY_RUN=1`이면 Codex를 호출하지 않고 실행 계획만 출력한다. **검사는 하되 잠금·백업 같은 상태 변경은 하지 않는다**(스모크 테스트가 상태를 남기면 안 된다).
- 출력 이름은 `.log`/`.err`/`.forcebak`/`.lock`으로 끝나면 안 된다(**대소문자 무관** — macOS 기본 FS는 대소문자를 구분하지 않는다). 관리 경로가 심링크거나 서로 같은 실체(하드링크)면 거부한다. `FORCE` 실행은 `.log`·`.err`를 둘 다 치운다(낡은 진단 오인 방지).
- 성공 = 종료코드 0 **그리고** 출력 파일 존재 **그리고** 공백 아닌 내용. 종료코드만으로 ✓를 찍지 않는다. **단 이 셋은 "돌았다"까지만 보증한다** — 산출물이 요청에 답했는지는 회수한 쪽이 읽어야 한다(스크립트가 완료 메시지로 경고한다).
- 기본 실행은 `--profile headless`를 사용한다. `~/.codex/headless.config.toml`에서 `features.hooks=false`로 두어 사용자 설정·플러그인은 유지하면서 lifecycle hook만 끈다. 프로필 자체가 실패할 때만 **격리 재실행: `DIVVY_ISOLATE_CONFIG=1`** (`--ignore-user-config`, 인증은 유지).
- 부모 Codex App의 OMX Stop hook이 `identity-indeterminate`를 반복 주입하면 [`scripts/omx_stop_hotfix.py`](./scripts/omx_stop_hotfix.py)로 **명시적으로** `status` 후 `apply`한다([upstream #3420](https://github.com/Yeachan-Heo/oh-my-codex/issues/3420)). `dispatch.sh`에서 자동 적용하지 않는다. 이 도구는 OMX 0.20.4의 정확한 source/dist 형태만 수정하고 백업·멱등 적용·`restore`를 제공한다. upstream 공식 수정이 배포되면 로컬 패치 대신 그 버전을 쓴다.
- **위임이 도는 동안 작업루트를 고치지 마라.** Codex는 스테이징 사본이 아니라 **live 파일**을 읽는다 — 실행 중 편집하면 무엇을 보고 판정했는지 재현할 수 없다.
- 종료코드: `1` 입력·경로 문제 · `2` 환경변수 값 오류 · `3` 기존 산출물·고립 백업 · `4` 실행 실패 · `5` 쓰기 승인 없음 · `6` 동일 출력 동시 실행 · `7` 경로 충돌·관리 이름 오용 · `8` 정리 실패 — **다른 실패보다 우선해 보고한다**(원래 rc는 메시지에 남는다) · `9` `DIVVY_EXPECT` 산출 계약 오류·미충족. rc 8에서는 산출물이 유효할 수 있으나 남은 상태를 봐야 한다.
- **성공 경로에서도** 그룹 잔존을 확인한다 — 직속 child가 끝났어도 같은 그룹의 자손이 남아 있으면 유예 후 TERM·KILL로 정리하고 나서 성공을 선언한다(완료 보고 뒤에 workspace가 계속 바뀌면 다음 실행과 겹친다).
- 중단(INT·TERM)은 Codex **프로세스 그룹**에 전달되고(자손까지), 유예 동안 그룹이 비기를 기다린 뒤 안 비면 KILL한다. 그 경로도 실패 경로와 같은 정리를 지난다(백업 복구·불완전 산출물 제거·잠금 해제). **경계**: `setsid`로 세션을 이탈한 자손에는 닿지 않는다 — 그런 작업은 read-only로 돌리거나 브리프에서 detached 실행을 금지한다.
- **쓰기 샌드박스에서는 출력을 작업루트 안에 두지 않는다**(스크립트가 rc 7로 거부). 위임된 작업이 잠금·백업을 지울 수 있다. 잠금에는 소유 토큰이 있어 남의 예약을 해제하지 않는다.

## 규칙

- 배정은 하되 **일을 두 번 하지 않는다**. G4가 검토자를 붙이지 않았으면 같은 일을 양쪽에 돌리지 않는다.
- **자기검토 금지.** 검토자는 항상 primary의 반대 러너다.
- **자기채점 금지.** 어느 러너가 잘했는지는 사람의 판정만 LEDGER에 들어간다. 내가 내 결과를 우수로 적으면 그 기록으로 ROSTER를 고칠 수 없다.
- ROSTER의 확신도를 섞지 않는다. 관찰된 도구(`[사실]`), 거기서 따라오는 결론(`[도출]`), 측정된 우위(`[실측]`), 추측(`[가설]`)을 같은 확신으로 쓰지 않는다.
- 사용자가 이미 러너를 지정했으면 **그 지정을 뒤집지 않는다**. 불일치가 보이면 한 줄로 보고만 한다. 이때 `결정출처`는 `user`이고 **갈린 게이트는 없다** — 게이트를 사후에 붙여 정당화하지 않는다.
- 브리프에는 **결정된 것**을 적는다. 이미 내린 결정을 CODEX가 재논의하면 회수 비용이 배정 이득을 먹는다.
- 실패 은폐 금지. CODEX가 빈 출력을 냈으면 "못 찾았다"가 아니라 "돌지 않았다"로 구분해 보고한다.

## 산출물 형식

```text
divvy 판정 (작업 N건)

| # | 작업 | primary | 결정출처 | 측정값(브리프줄/실행패스) | 근거 행 | G4 | 검토자 |
|---|------|---------|----------|---------------------------|---------|----|--------|
| 1 | ...  | CODEX   | G2       | 6줄 / 3패스               | —       | 低 | 없음   |
| 2 | ...  | CLAUDE  | G1       | 40줄 / 1패스              | T-3     | 中 | CODEX  |
| 3 | ...  | CLAUDE  | G3       | 12줄 / 1패스              | K-2, C-6[가설] | 高 | CODEX (peer-review 위임) |
| 4 | ...  | CODEX   | user     | 8줄 / 1패스               | —       | 低 | 없음   |

노출 확인: <사람 답변 또는 해당 없음>
실행: CODEX n건 → <경로>, CLAUDE m건 → 내가 수행
분포(LEDGER 누적): CLAUDE primary x / CODEX primary y / 반대 러너 검토가 **실행된** 건 z / G4 blocked w
결정출처별: G1 a, G2 b, G3 c, tie d, user e
```

`결정출처`는 `G1|G2|G3|tie|user` 중 하나다. LEDGER의 열 이름·집계와 같은 말을 쓴다.

## 검증

끝내기 전에 확인한다.

- 작업 단위마다 **primary가 정확히 하나**이고, **결정출처가 하나**, **G4 등급이 별도로** 적혀 있다. 결정출처가 `user`인 건은 갈린 게이트가 없는 것이 정상이다(그 예외를 인정한다).
- G4가 中·高인 건에 **검토자가 primary의 반대 러너**로 적혀 있다(자기검토 없음). 선행조건 불충족은 `G4 blocked`로 적혀 있고, 대체 경로(①②③)와 그 독립성 한계가 명시돼 있다 — 약한 대체를 "독립 검토 완료"로 적지 않았다.
- G2 판정에 라벨뿐 아니라 **측정값**(줄 수·패스 수)이 있고, "브리프 큼 + 실행 김"은 실제로 줄여본 결과와 판단 이유가 적혀 있다.
- 근거 인용이 규약대로다: G1은 `T-n`(한쪽이 못 하는 것만), G3는 `C-n`/`K-n`. `[실측]`·`[가설]` 행을 근거로 썼으면 등급을 표시했다.
- 비공개·규제 자료가 사람 확인 없이 위임되지 않았고, 답이 LEDGER `노출확인`에 남았다.
- CODEX 실행 건마다 성공 판정 3조건 결과가 보고되었고, 실패가 성공으로 반올림되지 않았다.
- LEDGER에 `L-<nn>` 행이 추가되었고, 우열 칸은 사람이 채우도록 비어 있다.
- LEDGER를 갱신했으면 `python3 scripts/ledger_distribution.py --check LEDGER.md`가 통과한다. 완료·종료된 배정만 세고, `CLAUDE+CODEX` 동시 primary는 각 러너에 중복 산입하지 않으며, 검토자 칸에 `**실행됨**`이 명시된 건만 실행된 검토로 센다.
- 리뷰 루프가 필요한 건은 divvy가 직접 라운드를 돌리지 않고 `peer-review`로 넘겼다.

## 연계

- 티어·추론량이 먼저 필요하면 → `modelchk`(모델 중립 판정) → 그 결과를 G3 입력으로.
- G4가 검토자를 붙였으면 → `peer-review`(라운드·triage 정본, 검토자 방향 지정).
- 배정 결과가 "여러 직무 관점 검토"면 → `cross-functional-review`.
- LEDGER가 쌓여 ROSTER를 고칠 때 → `meta-learning-loop`(예측 봉인 → 대조 → 단일 변수).
