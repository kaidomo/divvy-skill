# divvy ROSTER — 러너 명부

divvy의 G1(도구)·G3(적성) 판정 입력. **행 ID로 인용한다**(G1은 `T-n`, G3는 `C-n`/`K-n`).

확신도를 **네 등급**으로 구분해 적는다. 섞으면 이 파일로 판정을 고칠 수 없다.

| 등급 | 뜻 | 반박·승격 |
|---|---|---|
| `[사실]` | 사용하는 환경에서 **직접 관찰한** 도구·접근 사실 | 재확인으로 |
| `[도출]` | 관찰된 도구 사실에서 **필연적으로 따라오는** 배정 결론(= 한쪽이 **못 한다**) | 근거 T 행이 바뀌면 같이 무너진다 |
| `[실측]` | LEDGER에 사람 판정이 **같은 성격 3건 이상** 한 방향으로 쌓여 확인된 우위 | 반증이 쌓이면 강등 |
| `[가설]` | 적성 추측. 측정 없음 | LEDGER 누적으로 승격(`[실측]`) 또는 삭제 |

**G3 우선순위는 세 계층이다: `P1 = [사실]·[도출]` > `P2 = [실측]` > `P3 = [가설]`.** 불가능은 측정을
이긴다 — `[도출]`은 "한쪽이 못 한다"이고 `[실측]`은 "이쪽이 더 나았다"다. 비교는 **가장 높은 계층에서만**
하고, 그 계층에 양쪽 러너의 행이 다 있으면 **등급 이름이 달라도 무결정**이다(개수로도 이기지 않는다).

`[도출]`은 "한쪽이 못 한다"까지만 말한다. "한쪽이 더 잘한다"는 측정 전에는 `[가설]`, 측정 후에는
`[실측]`이며 **결코 `[사실]`·`[도출]`로 올라가지 않는다** — 그 둘은 도구 관찰과 그 귀결에만 쓰는 등급이다.

기본 예시 마지막 확인: 2026-07-31 (`codex-cli 0.146.0`). 설치한 환경에서 T행을 다시 확인한 뒤 사용한다.

---

## T. 도구 게이트 (G1 입력, 전부 `[사실]`)

| ID | 항목 | CLAUDE | CODEX |
|----|------|--------|-------|
| T-1 | 이 세션의 대화 맥락·`memory/` | 있음 | 없음(프롬프트로 옮겨야 함) |
| T-2 | `~/.claude/skills` 스킬 생태계 | 있음 | 없음 |
| T-3 | 설정된 MCP(Atlassian·Figma 등) | 있음 | 없음(Codex 자체 MCP는 별도 설정) |
| T-4 | 브라우저 자동화(Chrome) | 있음 | 없음 |
| T-5 | 서브에이전트 팬아웃·Workflow | 있음 | 없음 |
| T-6 | 샌드박스 실행 | 있음, 단 **대화형 승인** 경유 | `codex exec --sandbox`·`codex sandbox` — **승인 프롬프트 없이** 돌 수 있는 부분만 CODEX 독점 |
| T-7 | 구조화 출력 강제 | 스키마 기반 도구 있음 | `--output-schema`·`--json`·`-o` — **양쪽 다 가능하므로 독점 아님**(G1에서 갈리지 않는다) |
| T-8 | 비대화·무인 배치 실행 | 세션·사람 필요 | `codex exec`(헤드리스) — **CODEX 독점** |
| T-9 | 리포 코드리뷰 전용 진입점 | 범용 | `codex review` / `codex exec review` |
| T-10 | 과금 | 20x 정액 | 20x 정액 |
| T-11 | 노출면 | Anthropic | OpenAI (**러너 추가 = 노출면 추가**) |
| T-12 | 자기 환경이 런을 중단시킬 수 있나 | 세션 안에서 바로 보임 | **있음** — 기본 `--profile headless`가 lifecycle hook을 끈다. 프로필 실패 시에만 `--ignore-user-config` 완전 격리(2026-07-31 3조건×2회 실측: profile 2/2 통과·Stop/block 0) |

T-10 때문에 **비용은 divvy의 판정 기준이 아니다**. T-11 때문에 비공개·규제 자료는 위임 전 사람 확인이 필요하다.
T-12 때문에 **회수한 산출물은 내용을 읽어야 한다** — 성공 3조건(rc 0·존재·비공백)은 "돌았다"까지만 보증한다.

---

## C. CODEX 쪽으로 기우는 일

| ID | 일의 성격 | 근거 | 확신도 |
|----|-----------|------|--------|
| C-1 | 내 산출물의 독립 검토·반박(2nd opinion) | 다른 모델이라 사각이 겹치지 않는다. 같은 모델의 자기검토는 통과 편향을 못 깬다 | `[도출]` (독립성. 검토 품질 우위 주장은 아님) |
| C-2 | 브리프가 짧고 실행이 긴 일(장시간 무인 루프) | T-8·T-10 — 헤드리스로 오래 돌 수 있고 추가 과금이 없다 | `[도출]` |
| C-3 | 단일 리포 안에서 파일만 읽고 끝나는 조사·대조 | T-1 맥락 이전이 거의 필요 없다(=G2 유리). 품질 우위는 미확인 | `[가설]` |
| C-4 | **무인 배치**에서 스키마가 강제된 산출을 내야 하는 일 | T-7 + T-8 — 스키마 자체는 양쪽에 있으므로(T-7 독점 아님) 무인 배치와 결합될 때만 CODEX 쪽이다 | `[도출]` |
| C-5 | 사람 없이 비대화로 실행·재현해야 하는 일 | T-6(비대화 부분)·T-8 — CLAUDE의 실행은 대화형 승인에 묶인다 | `[도출]` |
| C-6 | 정밀 diff/패치 작성 | 실측 없음 | `[가설]` |

## K. CLAUDE 쪽으로 기우는 일

| ID | 일의 성격 | 근거 | 확신도 |
|----|-----------|------|--------|
| K-1 | 이 세션에서 쌓인 맥락·결정이 전제인 일 | T-1 (CODEX는 접근 불가) | `[도출]` |
| K-2 | 스킬 파이프라인을 타는 일(문서 저술·매뉴얼·스토리보드·PRD) | T-2 | `[도출]` |
| K-3 | Jira·Confluence·Figma가 소스인 일 | T-3 | `[도출]` |
| K-4 | 화면을 봐야 아는 일 | T-4 | `[도출]` |
| K-5 | 여러 관점 동시 팬아웃이 본질인 일 | T-5 | `[도출]` |
| K-6 | 이 레포 관례(브랜치·PR 본문 한/영·README 갱신)를 따라야 하는 일 | T-2 + 세션 맥락(T-1) | `[도출]` |
| K-7 | 한국어 산출물의 어조·용어 일관성 유지 | 실측 없음 | `[가설]` |

> 충돌 규칙(상단과 동일): `P1 = [사실]·[도출]` > `P2 = [실측]` > `P3 = [가설]`, 최상위 계층에 양쪽 행이 다 있으면 무결정(타이브레이커로).

---

## 고치는 법

1. LEDGER에 사람이 표시한 우열이 **같은 성격의 일에서 3건 이상** 한 방향으로 쌓이면 해당 행을 고친다.
   근거로 LEDGER의 `L-<nn>` ID를 적는다.
2. `[가설]`이 그렇게 확인되면 `[실측]`으로 올리고 근거 `L-<nn>`을 적는다.
   `[사실]`·`[도출]`로는 올리지 않는다 — 그 둘은 도구 관찰과 그 귀결에만 쓰는 등급이다.
3. 반증이 쌓이면 행을 지운다. 흔적을 남기려면 확신도를 내리고 근거에 반증 `L-<nn>`을 적는다.
4. 도구(T) 행은 버전이 바뀔 수 있다. 상단 "마지막 확인" 날짜와 `codex --version`을 함께 갱신한다.
   T 행이 바뀌면 그것에 기대던 `[도출]` 행을 같이 재검토한다.
