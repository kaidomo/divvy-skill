# divvy LEDGER — 배정 기록

divvy가 내린 배정과 그 결과를 기록한다. 공개 배포본은 개인 작업 이력을 포함하지 않는 빈 장부로 시작한다.

## 기록 규칙

- 첫 ID는 `L-01`이다. 이후 ID는 표에서 가장 큰 값에 1을 더하며 재사용·재번호하지 않는다.
- `근거행`에는 실제 판정에 쓴 ROSTER 행을 모두 적는다.
- `결과(primary)`와 `결과(검토)`는 분리한다.
- `우열`은 사람만 채운다. 러너가 자기 결과를 채점하지 않는다.
- 미위임 작업도 기록하고 `미위임사유`를 남긴다.
- `결과(primary)`의 선두 상태는 `완료|부분|부분 완료|막힘|실패|종료|취소|미착수|진행 중` 중 하나를 굵게 적는다.

## 표

| L-id | 날짜 | 작업 | primary | 결정출처 | 측정값(브리프줄/실행패스) | 근거행 | G4 | 검토자 | 미위임사유 | 노출확인 | 결과(primary) | 결과(검토) | 우열(사람) |
|------|------|------|---------|----------|---------------------------|--------|----|--------|------------|----------|---------------|------------|------------|

`결정출처`는 `G1|G2|G3|tie|user`, `미위임사유`는 `도구부재|브리프비용|적성(G3)|노출보류|G4blocked|user` 중 하나다.

## 분포 (divvy가 보고할 때 갱신)

> 이 절의 `- ` 불릿은 `scripts/ledger_distribution.py`가 소유한다.

러너별:

- CLAUDE primary: 0건
- CODEX primary: 0건
- CLAUDE+CODEX 동시 primary: 0건
- 반대 러너 검토가 **실행된** 건: 0건
- G4 blocked(中·高인데 검토를 못 돌린 건): 0건

결정출처별 — 왜 그렇게 갈렸는지는 여기서만 보인다:

- G1(도구): 0건
- G2(브리프 비용): 0건
- G3(적성): 0건
- tie(타이브레이커): 0건
- user(사용자 지정): 0건

미위임 사유별(CODEX로 가지 않은 이유):

- 도구부재(T-1~T-5): 0건
- 브리프비용 큼: 0건
- 적성(G3): 0건
- 노출보류: 0건
- G4blocked: 0건
- user: 0건

> 완료·종료된 배정만 센다. 표를 수정한 뒤에는
> `python3 scripts/ledger_distribution.py --check LEDGER.md`로 집계를 검증한다.
